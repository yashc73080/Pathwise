import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pathwise.app',
  appName: 'Pathwise',
  webDir: 'out',
  // Uncomment for live reload during development and add computer's IP address:
  // server: {
  //   url: ``,
  //   cleartext: true
  // }
};

export default config;
